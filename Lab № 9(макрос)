#include <stdio.h>
#include <cstdlib>
#include <iostream>
#include <string.h>
using namespace std;
int main()
{
char s[100];
gets(s);
for(int i=0;i<strlen(s);i++)

if((s[i]=='+')||(s[i]=='-')||(s[i]=='=')||(s[i]=='*')){
cout<<"yes,"<<s[i]<<" is here"<<endl;}

system("PAUSE");
return EXIT_SUCCESS;
}
